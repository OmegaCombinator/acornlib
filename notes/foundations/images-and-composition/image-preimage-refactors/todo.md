# Image/Preimage Refactors

Goal: replace theorem-local witness arguments with the generic image/preimage API where that genuinely simplifies maintenance.

This branch is complete. The remaining `set_ext` uses on image/preimage statements are the foundational helper theorems in `set.ac`.
