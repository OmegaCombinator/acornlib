# Set Ext Refactors

Goal: eliminate remaining non-foundational `set_ext` bridges where the generic set API can be used directly.

This leaf is complete. After the `image_of_range` refactor, only foundational `set_ext` proofs in `set.ac` remain.
