# Unary Composition

Goal: make ordinary unary function composition reusable as a first-class API instead of an inline pattern.

Completed. The core composition suite is in `util.ac`; no immediate downstream proof was a clean refactor target yet. Continue from [the parent notes](../todo.md).
