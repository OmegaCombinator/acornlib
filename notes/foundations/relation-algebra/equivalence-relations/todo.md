# Equivalence Relations

Goal: make equivalence relations usable as a first-class abstraction instead of just a conjunction of three predicates.

This leaf is complete. `util.ac` now has direct element-level equivalence lemmas plus `respects_equivalence`, and `set.ac` now has equivalence classes, class comparison lemmas, and saturated-set helpers.
