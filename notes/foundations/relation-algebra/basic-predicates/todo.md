# Basic Relation Predicates

Goal: stabilize the core API around reflexive, symmetric, transitive, antisymmetric, and equivalence relations before adding larger relation constructions.

This leaf is complete. The reusable relation API is in `util.ac`, and `ordered_group.ac` plus `add_ordered_group.ac` now use the generic strict-order helpers.
