# Logic Rewriting and Simplification

Goal: make common logical rewrites predictable enough that proofs stop needing repetitive boilerplate.

- [ ] Add iff-based rewriting helpers beyond implication and negation transport
- [ ] Add normalization lemmas for set-membership propositions
- [ ] Collect existing local logic helper lemmas into a central API
- [ ] Refactor representative proofs to measure whether the new logic API reduces proof length
- [ ] Expand contradiction helpers if downstream proofs expose missing patterns
